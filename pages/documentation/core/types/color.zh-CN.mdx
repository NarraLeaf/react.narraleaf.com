# color

```ts
type color = string | {
    r: number;
    g: number;
    b: number;
    a?: number;
};
type Color = {
    color: color;
};
```

The color type can be a string or an object with `r`, `g`, `b`, and optionally `a` properties.


