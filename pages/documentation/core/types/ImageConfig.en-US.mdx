# ImageConfig

```typescript
type ImageConfig = {
    src: string | StaticImageData;
} & CommonDisplayable;
```

The configuration of the image.

For CommonDisplayable, see [CommonDisplayable](./CommonDisplayable).

## Public Properties

### src

The source of the image. It can be a URL or a StaticImageData.
