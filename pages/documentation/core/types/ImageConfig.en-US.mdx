# ImageConfig

```typescript
type ImageConfig = {
    src: string | StaticImageData;
} & CommonImage;
```

The configuration of the image.

For CommonImage, see [CommonImage](./CommonImage).

## Public Properties

### src

The source of the image. It can be a URL or a StaticImageData.
