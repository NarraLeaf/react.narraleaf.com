# Utils

## Word

```typescript
import {i, c, b} from "narraleaf-react"
```

These functions are equivalent to the following:

- `i` is equivalent to `Word.italic`
- `c` is equivalent to `Word.color`
- `b` is equivalent to `Word.bold`
