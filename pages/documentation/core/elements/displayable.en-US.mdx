import {Callout, Steps} from "nextra/components";

# Displayable

Displayable defines all the properties and methods that a displayable element should have. Displayable elements are elements that can be displayed on the screen.  
Ex. Image, Text

## Chainable Methods

### pos

<Steps>

Transform the element to a new position.

```typescript
element.pos({
    xalign: 0.3
}, 1000, "linear"); // Move the element to x: 0.3 in 1000ms with linear ease.
```

- `position: CommonDisplayableConfig["position"]` - The new position of the element, see [CommonDisplayableConfig](../types/CommonDisplayable#position)
- `duration?: number` - The duration of the transform in milliseconds
- `easing?: TransformDefinitions.EasingDefinition` - The easing function to use, see [TransformDefinitions](../types/TransformDefinitions).[EasingDefinition](../types/TransformDefinitions#EasingDefinition)

</Steps>

### scale

<Steps>

Scale the element.

```typescript
element.scale(1.5, 0.8, 1000, "easeInOut"); // Scale the element to 1.5x on X-axis and 0.8x on Y-axis in 1000ms with easeInOut ease.
```

- `scaleX: number` - The new scale of the element on the X-axis, use negative value to invert the scale
- `scaleY: number` - The new scale of the element on the Y-axis, use negative value to invert the scale
- `duration?: number` - The duration of the transform in milliseconds
- `easing?: TransformDefinitions.EasingDefinition` - The easing function to use, see [TransformDefinitions](../types/TransformDefinitions).[EasingDefinition](../types/TransformDefinitions#EasingDefinition)

</Steps>

### scaleX

<Steps>

Scale the element on the X-axis.

```typescript
element.scaleX(1.5, 1000, "easeInOut"); // Scale the element to 1.5x on X-axis in 1000ms with easeInOut ease.
```

- `scaleX: number` - The new scale of the element on the X-axis
- `duration?: number` - The duration of the transform in milliseconds
- `easing?: TransformDefinitions.EasingDefinition` - The easing function to use, see [TransformDefinitions](../types/TransformDefinitions).[EasingDefinition](../types/TransformDefinitions#EasingDefinition)

</Steps>

### scaleY

<Steps>

Scale the element on the Y-axis.

```typescript
element.scaleY(0.8, 1000, "easeInOut"); // Scale the element to 0.8x on Y-axis in 1000ms with easeInOut ease.
```

- `scaleY: number` - The new scale of the element on the Y-axis
- `duration?: number` - The duration of the transform in milliseconds
- `easing?: TransformDefinitions.EasingDefinition` - The easing function to use, see [TransformDefinitions](../types/TransformDefinitions).[EasingDefinition](../types/TransformDefinitions#EasingDefinition)

</Steps>

### scaleXY

<Steps>

Scale the element on both X and Y axes.

```typescript
element.scaleXY(1.2, 0.9, 1000, "easeInOut"); // Scale the element to 1.2x on X-axis and 0.9x on Y-axis in 1000ms with easeInOut ease.
```

- `scaleX: number` - The new scale of the element on the X-axis
- `scaleY: number` - The new scale of the element on the Y-axis
- `duration?: number` - The duration of the transform in milliseconds
- `easing?: TransformDefinitions.EasingDefinition` - The easing function to use, see [TransformDefinitions](../types/TransformDefinitions).[EasingDefinition](../types/TransformDefinitions#EasingDefinition)

</Steps>

### zoom

<Steps>

Zoom the image (recommended for image scaling).

```typescript
element.zoom(2, 1000, "easeInOut"); // Zoom the image to 2x in 1000ms with easeInOut ease.
```

- `zoom: number` - The new zoom level of the image
- `duration?: number` - The duration of the transform in milliseconds
- `easing?: TransformDefinitions.EasingDefinition` - The easing function to use, see [TransformDefinitions](../types/TransformDefinitions).[EasingDefinition](../types/TransformDefinitions#EasingDefinition)

</Steps>

### rotate

<Steps>

Rotate the element.

```typescript
element.rotate(90, 1000, "easeInOut"); // Rotate the element to 90 degrees in 1000ms with easeInOut ease.
```

- `angle: number` - The new angle of the element in degrees
- `duration?: number` - The duration of the transform in milliseconds
- `easing?: TransformDefinitions.EasingDefinition` - The easing function to use, see [TransformDefinitions](../types/TransformDefinitions).[EasingDefinition](../types/TransformDefinitions#EasingDefinition)

</Steps>

### opacity

<Steps>

Change the opacity of the element.

```typescript
element.opacity(0.5, 1000, "easeInOut"); // Change the opacity of the element to 0.5 in 1000ms with easeInOut ease.
```

- `opacity: number` - The new opacity of the element, between 0 and 1
- `duration?: number` - The duration of the transform in milliseconds
- `easing?: TransformDefinitions.EasingDefinition` - The easing function to use, see [TransformDefinitions](../types/TransformDefinitions).[EasingDefinition](../types/TransformDefinitions#EasingDefinition)

</Steps>

### transform

<Steps>

Apply a custom transform to the element.

```typescript
element.transform(new Transform(
    /* Transform Definitions */
));
```

- `transform: Transform` - The custom transform to apply to the element, see [Transform](../animation/transform)

</Steps>

### show

<Steps>

#### Overload 1 of 3

Show the element immediately.

```typescript
element.show();
```

#### Overload 2 of 3

Show the element with custom transform.

```typescript
element.show({
    /* Transform Definitions */
});
```

- `options: Transform` - The custom transform to apply to the element, see [Transform](../animation/transform)

#### Overload 3 of 3

Show the element with transform config. A transform with alpha:1 will be applied to the element.

```typescript
element.show({
    duration: 1000,
    ease: "easeInOut",
});
```

- `options: Partial<TransformDefinitions.CommonTransformProps>` - See [TransformDefinitions](../types/TransformDefinitions).[CommonTransformProps](../types/TransformDefinitions#CommonTransformProps)

</Steps>

### hide

<Steps>

#### Overload 1 of 3

Hide the element immediately.

```typescript
element.hide();
```

#### Overload 2 of 3

Hide the element with custom transform.

```typescript
element.hide({
    /* Transform Definitions */
});
```

- `options: Transform` - The custom transform to apply to the element, see [Transform](../animation/transform)

#### Overload 3 of 3

Hide the element with transform config. A transform with alpha:0 will be applied to the element.

```typescript
element.hide({
    duration: 1000,
    ease: "easeInOut",
});
```

- `options: Partial<TransformDefinitions.CommonTransformProps>` - See [TransformDefinitions](../types/TransformDefinitions).[CommonTransformProps](../types/TransformDefinitions#CommonTransformProps)

</Steps>

### bringToFront

<Steps>

Raise the element to the front of the layer it is on. Available on [Image](./image), [Text](./text) and Puppet.

```typescript
scene.action([
    yukoSprite.bringToFront(),
    yuko.say`It was me, all along.`,
]);
```

The move takes effect at once and has no options. Nothing else about the element changes: same layer, same transform, same source. The resulting order is part of the saved game, and a save taken afterwards restores it.

- Takes no arguments

</Steps>

<Callout type="warning">
`bringToFront` only orders elements **within one layer**. It cannot lift an element above one on a higher layer, because layers are composited by z-index and that comparison is applied first. Use [`Layer.setZIndex`](./layer#setzindex) to move a whole layer forward.
</Callout>

<Callout type="info">
There is no `sendToBack`. To arrange a group, bring the elements to the front in the order you want them, back to front.
</Callout>

[`Layer`](./layer) and [`Camera`](./camera) both extend Displayable but throw a `RuntimeGameError` from this method. Neither is an element inside a layer, so neither has a front to be moved to. The error is raised while the story is being built, not during playback.




