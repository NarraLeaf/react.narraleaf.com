import {Callout, Tabs, Steps} from 'nextra/components';

# 选项

<Callout type="warning" emoji="⚠️">
    本页面正在建设中
</Callout>

`菜单` 允许您在视觉小说中创建菜单。它用于在故事的不同部分之间导航。

```ts
Menu.prompt("我应该做什么？")
    .choose("往左走", [
        character1.say("我往左走了"),
        scene1.jumpTo(scene2)
    ])
    .choose("往右走", [
        character1.say("我往右走了")
    ])
```

## 静态方法

### prompt

<Steps>

- `prompt: SentencePrompt | Sentence | null | undefined` - 见 [SentencePrompt](../types/SentencePrompt) 或 [Sentence](./character/sentence)
- `config: MenuConfig` - [MenuConfig](../types/MenuConfig)

</Steps>


## 公共方法

### constructor

<Steps>

#### 重载 1 / 2

- `prompt: SentencePrompt` - 见 [SentencePrompt](../types/SentencePrompt)
- `config?: MenuConfig` - [MenuConfig](../types/MenuConfig)

#### 重载 2 / 2

- `prompt: Sentence` - 菜单的提示，见 [Sentence](./character/sentence)
- `config?: MenuConfig` - [MenuConfig](../types/MenuConfig)

</Steps>

## 链式方法

### choose

创建一个菜单分支。选择提示可以是句子。

<Steps>

#### 重载 1 / 3

```ts
new Menu("我应该做什么？")
    .choose({
        choice: "往左走",
        action: [
            character1.say("我往左走了"),
        ]
    })
```

- `choice: MenuChoice` - 菜单的选择，见 [MenuChoice](../types/MenuChoice)

#### 重载 2 / 3

```ts
new Menu("我应该做什么？")
    .choose(new Sentence("往左走"), [
        character1.say("我往左走了"),
    ])
```

- `prompt: Sentence` - 选择的提示，见 [Sentence](./character/sentence)
- `action: ActionStatements` - 选择的操作，见 [ActionStatements](../types/ActionStatements)

#### 重载 3 / 3

```ts
new Menu("我应该做什么？")
    .choose("往左走", [
        character1.say("我往左走了"),
    ])
```

- `prompt: SentencePrompt` - 见 [SentencePrompt](../types/SentencePrompt)
- `action: ActionStatements` - 选择的操作，见 [ActionStatements](../types/ActionStatements)

</Steps>
