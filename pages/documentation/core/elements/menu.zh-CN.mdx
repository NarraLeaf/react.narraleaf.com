import {Callout, Tabs, Steps} from 'nextra/components';

# 选项

<Callout type="warning" emoji="⚠️">
    This page is under construction.
</Callout>

<Callout type="warning" emoji="⚠️">
    Beta feature, subject to change.
</Callout>

`菜单` 允许您在视觉小说中创建菜单。它用于在故事的不同部分之间导航。

```ts
new Menu("我应该做什么？")
    .choose("往左走", [
        character1.say("我往左走了"),
        scene1.jumpTo(scene2)
    ])
    .choose("往右走", [
        character1.say("我往右走了")
    ])
```

## 公共方法

### constructor

<Steps>

#### 重载 1 / 2

- `prompt: (string | Word)[] | (string | Word)` - 菜单的提示
- `config?: MenuConfig` - [MenuConfig](../types/MenuConfig)

#### 重载 2 / 2

- `prompt: Sentence` - 菜单的提示，见 [Sentence](./character/sentence)
- `config?: MenuConfig` - [MenuConfig](../types/MenuConfig)

</Steps>

## 链式方法

### choose

<Steps>

#### 重载 1 / 3

```ts
new Menu("我应该做什么？")
    .choose({
        choice: "往左走",
        action: [
            character1.say("我往左走了"),
        ]
    })
```

- `choice: MenuChoice` - 菜单的选择，见 [MenuChoice](../types/MenuChoice)

#### 重载 2 / 3

```ts
new Menu("我应该做什么？")
    .choose(new Sentence("往左走"), [
        character1.say("我往左走了"),
    ])
```

- `prompt: Sentence` - 选择的提示，见 [Sentence](./character/sentence)
- `action: ChainedActions` - 选择的操作，见 [ChainedActions](../types/ChainedActions)

#### 重载 3 / 3

```ts
new Menu("我应该做什么？")
    .choose("往左走", [
        character1.say("我往左走了"),
    ])
```
- `prompt: UnSentencePrompt` - 选择的提示
- `action: ChainedActions` - 选择的操作，见 [ChainedActions](../types/ChainedActions)

</Steps>
