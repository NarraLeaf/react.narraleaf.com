import {Callout, Tabs, Steps} from 'nextra/components';

# 条件

<Callout type="warning" emoji="⚠️">
    This page is under construction.
</Callout>

<Callout type="warning" emoji="⚠️">
    Beta feature, subject to change.
</Callout>

`Condition` 允许你在故事中创建条件分支。

```typescript
new Condition()
    // 检查玩家是否有足够的钱
    .If(({ storable }) => {
        // 你可以访问游戏状态，例如：storable
        return storable.getNamespace("name").get("coin") >= 10;
    }, [
        character1.say("你有足够的钱")
    ])

    .Else([
        character1.say("你没有足够的钱")
    ])
```

> **注意:** 为了更好地理解脚本，如果你以错误的顺序调用这些方法，它将抛出错误。
> 
> 例如，你不能在 `If`之前调用`ElseIf`。

## 公共方法

### constructor

<Steps>

- `config?: ConditionConfig` - ConditionConfig

</Steps>

## 链式方法

### If

<Steps>

- `condition: Lambda | LambdaHandler<boolean>` - 要检查的条件。如果条件为真，将执行这些动作。
参见 Lambda 和 LambdaHandler 了解更多信息。
- `action: ChainedActions` - ChainedActions

</Steps>

### ElseIf

<Steps>

- `condition: Lambda | LambdaHandler<boolean>`
- `action: ChainedActions`

</Steps>

### Else

<Steps>

- `action: ChainedActions`

</Steps>
